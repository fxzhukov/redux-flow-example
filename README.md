# Shopping cart example (react, react-redux, flow)

### npm commands:
* `npm start` - starts development server
* `npm run flow` - runs flow typechecking
* `npm test -- --coverage` - runs tests and collect coverage